Log numbers 0-4 in two ways: (1) by extracting the setTimeout call, (2) by wrapping the anon. function in a function run immediately to bind variable i
--
var i;
for (i = 0; i < 5; i++) {
  setTimeout(function () {
    console.log(i);
  }, i*1000);
}
