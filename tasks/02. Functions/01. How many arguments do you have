Complete the sum function, making it sum all arguments, no matter how many you give it.
--

function sum () {
  var s = 0;

  return s;
}

assert(sum() === 0, 'sum without arguments should be 0');
assert(sum(1, 2) === 3, 'sum with arguments 1 and 2 should equal 3');
assert(sum(1, 2, 3) === 6, 'sum with arguments 1, 2, and 3 should equal 6');
