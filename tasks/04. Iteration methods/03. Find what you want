Using your forEach function build a odd number filter
--
var data = [1, 2, 3, 4, 5, 6, 7, 9, 10];

function isOdd(element, index, array) {
  return element % 2 == 1;
}

// Implement filter so that it returns an array with all the array elements that
// func returned true for.

function filter(array, func) {

}

var result = filter(data, isOdd);

console.log(result);

// How would you use this basis to create a filterOdd function?
