Print all the numbers in the array using your own forEach
--
var data = [1, 2, 3, 4, 5];

// Implement forEach so that it calls func with the current element, the index
// and the array on all the items.

function forEach(array, func) {

}

forEach(data, function(element, index, array) {
  console.log(element);
});

// What about hashes, does your forEach support traversing them?
