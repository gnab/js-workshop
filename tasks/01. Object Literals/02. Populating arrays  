Populate the array, please.
--
var arr = [];
var i;                
for (i = 0; i < 10; i++) {
  // ..
}
                         
assert(arr[0] === 0);
assert(arr[1] === 1);
assert(arr.length === 10)